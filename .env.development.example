# Server Configuration
NODE_ENV=development
LISTEN_HOST=localhost
LISTEN_PORT=8080
BASE_PATH=
LOG_LEVEL=debug
TZ=UTC

# Processing Limits
# Maximum file size (25MB)
FILE_MAX_BYTES_MB=25
# Number of concurrent tasks (recommended = number of CPU cores)
MAX_CONCURRENCY=2
# Time to wait in queue before rejection (30 sec)
QUEUE_TIMEOUT_SECONDS=30
# Total request timeout including queue wait (60 sec)
REQUEST_TIMEOUT_SECONDS=60

# Shutdown
# Graceful shutdown timeout for active tasks (30 sec)
SHUTDOWN_TIMEOUT_SECONDS=30